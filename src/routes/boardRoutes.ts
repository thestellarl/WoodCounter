import { Router } from "express";
// import { BoardController } from "../controllers/boardController";

const router = Router();
// const boardController = new BoardController();

// router.get("/job-stats", boardController.getJobStats.bind(boardController));
// router.delete("/boards/:id", boardController.removeBoard.bind(boardController));
// router.put(
//   "/boards/:id/archive",
//   boardController.archiveBoard.bind(boardController)
// );

export default router;
